<nav class="navbar navbar-default navbar-fixed-top bg-navy-blue" role="navigation">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="<%= link_by_signed_in %>">Buck<span class="theme-blue">it</span></a>
        </div>

        <div class="collapse navbar-collapse navbar-ex1-collapse">
            <ul class="nav navbar-nav navbar-right">
                <% unless signed_in? %>
                    <li>
                        <%= link_to 'Home', root_path, class: 'page-link', id: "home-link" %>
                    </li>
                <% end %>
                <li>
                    <%= link_to 'Documentation', doc_path, class: 'page-link' %>
                </li>
                <% if signed_in? %>
                    <li>
                        <%= link_to 'Your Applications', oauth_applications_path, class: 'page-link' %>
                    </li>
                    <li>
                        <%= link_to 'Log Out', logout_path, class: 'page-link', method: :delete %>
                    </li>
                <% end %>
                <% unless signed_in? %>
                    <li>
                        <%= link_to 'Log In | Sign Up', register_path, class: 'page-link' %>
                    </li>
                <% end %>
            </ul>
        </div>
    </div>
</nav>